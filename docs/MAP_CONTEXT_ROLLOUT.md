# Existing Country geographic-context migration — handoff

Status (2026-09-22): **DRAFT / STAGE ② GEOGRAPHIC QA IN PROGRESS / STAGE ③ HOLD / NOT RELEASED**. PR #935 is draft and unmerged. The earlier statement "STAGE ② COMPLETE / STAGE ③ READY" was incorrect: excluding four maps does not complete geographic QA for the requested complete-Country roster. The former blanket PASS assertions for the other maps are withdrawn. This correction supersedes the now-obsolete Stage ② COMPLETE paragraph in this document's history; [the earlier detailed, pre-closure source and diagnostic record remains available at its immutable revision](https://github.com/yagenji/Journey-Atlas/blob/54c8c7f26abbfc4f25264b65f529cef693ab9825/docs/MAP_CONTEXT_ROLLOUT.md), with dated details also in the PR discussion. Never silently discard that evidence or describe CI as geographic acceptance.

## Scope and map language

Canonical registry: `data/atlas-destinations.json` (201 destinations). Derive the current finished/published roster from this registry and Country Production States each time, rather than hardcoding a historical total. Retain each approved target-country path, marker, image, Country JSON, production/publication setting, 1200×760 canvas and Iceland/Norway map language. Existing outlines stay dominant; sea `#eaf2f4` → `#dcebf0` → `#d0e3eb`, surrounding land `#e4e0ce`, coast stroke `#b6bbaf`. No invented coastlines, administrative boundaries, padding, geographic deletion, or unlabeled change of source/vintage. New-Country context specification on main (#950) is separate; do not overwrite it or migrate in-progress Countries.

## Verified technical baseline — not geographic signoff

At the most recently audited roster snapshot (2026-09-22; see [inventory run 35709489153](https://github.com/yagenji/Journey-Atlas/actions/runs/35709489153)), 109 selected completed/published maps comprised **94 newly generated preview SVG/PNG pairs and 15 preserved already-context maps**. The 94 generated SVGs have matching report SHA-256, valid XML and `viewBox="0 0 1200 760"`; all 94 PNGs fully decode at 1200×760. The [preflight run 35719353622](https://github.com/yagenji/Journey-Atlas/actions/runs/35719353622) also passed real-page Desktop / Tablet / Mobile browser QA for the disposable staged set and OSM/ODbL visible-attribution checks. Those checks do **not** independently establish real coastlines, border topology or source compatibility. Fifteen preserved maps did not appear as fresh preview pairs and must not be counted among the 94.

The common generator's surrounding-land source is Basemap 2.0.0 / GSHHG 2.3.6. Its source/version and redistribution obligations require retention. OSM-derived exceptions are ODbL; the shared `assets/js/map-source-credit.js` and existing focused browser test are the attribution route. A source name alone is not a geographic verification. Before any Stage ③ action, recalculate the roster and exact source hashes from the latest main and the PR head; those can advance independently.

## Geographic QA still open and specifically resolved cases

**Bahrain — Stage ② individual geographic QA PASS for the migration candidate:** original approved source is `assets/images/bahrain/map-atlas-v3.svg`, not v1. The previously suspicious pale main-map western wedge is **real Saudi Arabian mainland**, not an island artifact. `map.regions.main` WGS84 bounds 50.37–50.69°E / 25.78–26.30°N are *aspect-fitted* into rect x10–980 / y0–760; the visible canvas actually extends to 50.1606659809°E. Its x10–92.5 / y0–353.7 wedge (~15,766.955 SVG px²) matches the independent intermediate-resolution GSHHG WGS84 land polygon; a wider independent GSHHG query 49.5–51°E / 25.5–26.9°N confirms it is part of the same level-1 connected Saudi mainland reaching 49.7°E, 26.2°N. Its straight x10 edge is a legitimate viewport clip and must **not** be deleted, smoothed or filled with invented water. [Dated source audit](https://github.com/yagenji/Journey-Atlas/pull/935#issuecomment-5779103532); `tests/test_map_context_portugal_bahrain_sources.py` preserves this fact and approved target paths. For Hawar, the approved Bahrain target is geoBoundaries gbOpen BHR ADM0 `BHR-ADM0-88433195` (boundary year 2017, OSM/Wambacher, source update 2023-01-19, build 2023-12-12, ODbL), and the staged neighboring Qatar land is now the corresponding same-series QAT ADM0 `QAT-ADM0-15585745` with the same represented year/source-update/build date and ODbL provenance. The replacement changes **only generated foreign Hawar context**; both approved Bahrain national paths remain byte-identical. The earlier five GSHHG inset rings were reconciled so three Bahraini self-duplicates are removed and Qatar land is retained behind exact source/path guards. Bahrain SLRB independently confirms that its 2024 1:100,000 national map covers Bahrain plus sections of Saudi Arabia and Qatar and that a dedicated 2024 1:50,000 Hawar sheet exists. At JOURNEY ATLAS 1200×760 scale, the reviewed candidate therefore has source-backed neighboring land without an invented coast or deletion of approved Bahrain geometry. This PASS is cartographic acceptance for this migration candidate, not a survey-grade redefinition of Bahrain or Qatar.

**Hong Kong — Stage ② individual geographic QA PASS for the migration candidate:** the earlier disconnected GSHHG Shenzhen fragments have been replaced in the disposable existing-map candidate by a pinned OpenStreetMap `natural=coastline` mainland snapshot (ODbL), with all 18 approved Hong Kong land-only paths preserved exactly. The generated `hong-kong-foreign-only` clip is the exact complement of those protected paths, so the shared Shenzhen/New Territories land interface follows the already-approved Hong Kong geometry instead of an invented bridge or a second administrative source. The pinned mainland path SHA-256 is `d24687d3784f5d032cadd42d6cd359e86eaf697926358c4638a1b36fb0b2e3f9`; the full reviewed 1200×760 candidate is locked at SHA-256 `4304be66e354d8906501ea6e51e31ed60cd93f5f38ee20c7b47a56d3c8e7641e`. An independently retrieved later OSM coastline snapshot produced the same mainland topology with ~949.3 SVG px² symmetric difference over ~233,940.6 px² of mainland and a 0.637 SVG-px Hausdorff distance, which is within the existing 0.6px source-line simplification scale and does not justify redrawing the protected border. Lands Department's current digital topographic mapping covers the whole HKSAR, and the 2026 e-HongKongGuide detailed maps were updated to June 2026; those official references are used only as independent current-territory checks, not redistributed geometry. Visible OSM/ODbL attribution is handled by the shared source-credit path. This is a PASS for the JOURNEY ATLAS 1200×760 migration candidate, not cadastral or legal-boundary certification.

**Brunei — Stage ② individual geographic QA PASS after explicit domestic-island correction:** the earlier migration adapter had already separated genuine Malaysian/Sarawak mainland from Brunei self-overlap, but the protected Brunei map itself omitted Pulau Muara Besar. The island is independently confirmed as Brunei territory by Brunei government sources: BEDB operates the Pulau Muara Besar Industrial Site and JKNG places Pulau Muara Besar in Kampung Serasa, Mukim Serasa, Daerah Brunei dan Muara. Following explicit user direction to execute the Brunei correction, the existing mainland/Temburong target path remains byte-identical and a **second protected target path** is added only for Pulau Muara Besar from current OpenStreetMap coastline way 28531951 v9 (2025-03-01, ODbL 1.0), captured 2026-09-23 with raw SHA-256 `dd1bb1fbab630eb793b2c855f678fbc5a01f5ff21c1bdfdaf1410577669993d3` and projected path SHA-256 `b638398ba246e951a88e590a4cfc2f557a5cc9ad1c85b948fe2c93fb55655fa1`. The current OSM island is ~625.8 SVG px²; the older GSHHG island ring is ~429.2 px², overlaps 92.96% of its own area with the current island, and has ~17.08 SVG-px Hausdorff distance, consistent with substantial coastal/reclamation change rather than a different island. The old GSHHG island is therefore never colored as Malaysia; the source-backed Sarawak mainland remains the only pale foreign context, clipped against the now-complete three-component Brunei target. Country map source attribution now explicitly includes OSM/ODbL for PMB. This is a PASS for the JOURNEY ATLAS 1200×760 migration candidate, not cadastral or legal-boundary certification.

**Monaco — Stage ② individual geographic QA PASS for the migration candidate:** the stale five-vertex GSHHG diagonal is absent from the current Draft asset. The protected Monaco silhouette remains the approved OpenStreetMap relation 36990 path (including Mareterra), while surrounding France comes from OSM relations 174558, 174562, 174956, 174958 and 174959 under ODbL. Native-size comparison finds 1,211 Monaco-border samples within 5 SVG px of the independent French outline and 1,202 within 0.25px; the raw source polygons have only 13.744 SVG px² of overlap. The Draft map now applies `monaco-foreign-only`, an exact even-odd complement of the protected Monaco path, to **foreign context only**. This removes the residual display overlap without changing, extending, smoothing or reinterpreting either source geometry; seaward Monaco coastline remains the approved target. IMSEE's official territory statistics independently describe Monaco as roughly 2.08 km² with a finite land frontier and Mediterranean coast, consistent with this land/sea topology. The protected national path, French context paths and exact exclusion are regression-guarded. This is a PASS at JOURNEY ATLAS map scale, not cadastral or treaty-boundary certification.

**Portugal mainland/Spain — actual source-matched draft repair:** three mainland/Azores/Madeira viewports retain explicit frames. Earlier full-size mask comparison found Portuguese self-island overlap in the isolated ocean viewports; [commit `1be57ab`](https://github.com/yagenji/Journey-Atlas/commit/1be57ab211087ae4a066b74cc8f1404431283f83) removes only their redundant GSHHG context, leaving all 15 approved Portuguese country paths intact. The original Portugal map metadata names Natural Earth 1:10m through `LonnyGomes/CountryGeoJSONCollection`, PRT.geojson Git blob `ce02dabb0ea17eba11923f78ed1525d8989c9b58`. An [independent source-pinned CI diagnostic and 1200×760 preview](https://github.com/yagenji/Journey-Atlas/actions/runs/35745350179/artifacts/10702417852) compared the *same upstream collection’s* ESP.geojson Git blob `01ec685ca5739b63292e01380ff36287413508b5`, projected its closed Spanish land onto the unchanged mainland WGS84 viewport and showed why the prior GSHHG source-vintage halo/mismatched Spain coast must be replaced. The approved Portugal border differs from sibling source by a Hausdorff 0.0006161°; Spain-source polygon still overlaps the protected target by 0.00098524 square degrees. Thus this is source-aligned cartography, **not survey-grade or pixel-perfect agreement**; do not modify approved Portugal boundaries to force them to meet. [Commit `234f108`](https://github.com/yagenji/Journey-Atlas/commit/234f1083a696c0258f5a27b9ca74e685790d1cfd) replaces **only the generated mainland neighbor context path** with the checked Spain source path, pins input and output SHA, embeds complete source/provenance, and retains 15 original national paths, three frames, markers, Azores and Madeira. [Natural Earth use terms](https://www.naturalearthdata.com/about/terms-of-use/) confirm public-domain vector data. A source/topology regression is in [`2c9fc3f`](https://github.com/yagenji/Journey-Atlas/commit/2c9fc3f92cb3645a948479c548834bab80b5adc1); the temporary upstream-fetch diagnostic/workflow change was removed in [`447364b`](https://github.com/yagenji/Journey-Atlas/commit/447364b029caf9a0ff6d5c75da1f1e2facac29aa) after source hashes and artifact were captured. No production Portugal SVG or Country JSON replaced. Latest-head real page browser QA must pass before any release. Portugal’s specifically requested mainland/Spain source mismatch is repaired in the Draft migration; this is **not** a blanket signoff for the entire Country nor all 109 maps.

**Qatar — Stage ② individual geographic QA PASS for the migration candidate:** the approved national map and its neighboring-land candidate use the same Basemap 2.0.0 intermediate-resolution GSHHS/GSHHG + `countries_i` WGS84 source family. The migration correction does not replace Qatar with a different-vintage administrative polygon: it preserves the real shared Saudi mainland component and clips only the exact protected Qatar silhouette from generated foreign context. A separate geoBoundaries QAT 2017 comparison was intentionally **not** substituted because its different coastline/land-border vintage diverges materially from the approved map. Qatar CGIS independently publishes a polygon layer for “Qatar Land Boundary” and separate Saudi/small-island layers, which is topologically consistent with the retained peninsula + neighboring-mainland composition. The final 1200×760 candidate preserves both approved national paths and the Doha inset and contains no inferred coast, smoothing or reassigned national land. This is a PASS for the migration candidate, not survey-grade boundary certification.

**Kuwait — Stage ② individual geographic QA PASS for the migration candidate:** the national overview and generated neighboring land use the same Basemap/GSHHG intermediate-resolution WGS84 source family. Four generated island rings proven to overlap approved Kuwaiti islands by 99.64–100% are removed only from context; protected national paths and the Kuwait City inset remain unchanged. The sixth northern ring is deliberately retained: the independent KWT/IRQ geoBoundaries comparison gives **0 intersection with KWT** and 82.23% intersection with IRQ, while Kuwait Government Online identifies Warbah as the northernmost Kuwaiti island and the Kuwait GeoHub “Kuwait Boundaries with Islands” map (Alyaqout 2023 & OSM 2023) provides an independent boundary-with-islands sanity check. The residual source-vintage difference is not a basis for deleting or redrawing the neighboring ring. Therefore the reviewed candidate correctly avoids treating it as Kuwaiti self-land and preserves it as neighboring context. This is a PASS at JOURNEY ATLAS map scale; no approved Kuwait geometry is changed.

**Singapore — Stage ② individual geographic QA PASS for the migration candidate:** the approved national map was created after Singapore Land Authority's June 2025 National Map Polygon release and preserves five approved Singapore land paths. The reviewed foreign context uses pinned OpenStreetMap coastline ways dated 2026-09-20; the same SLA National Map Polygon snapshot independently classifies Singapore versus Johor coastal components before foreign-land construction. At native 1200×760 review, raw OSM foreign context overlaps the protected Singapore silhouette only where cross-source geometry intrudes (~515.5 SVG px²); the exact `foreign-land-only` clip removes that overlap without changing any approved path, and the rendered Johor / Riau context remains separated by the real straits except for infrastructure-scale crossings. Singapore has no shared mainland administrative border whose coastline must be made coincident, so using current OSM neighboring coast with the authoritative Singapore target does not require an invented seam. SLA identifies the National Map Polygon as including coastal outlines and licenses it under the Singapore Open Data Licence. The final context path is source/hash-guarded in `tests/test_map_context_exception_coasts.py`. This is a PASS for the JOURNEY ATLAS 1200×760 migration candidate, not cadastral or hydrographic certification.

**Macau — Stage ② individual geographic QA PASS for the migration candidate:** the protected Macau silhouette comes from the Macao SAR Government WebMap WGS84 polygon service and was added to the repository on 2026-09-09; DSSCU states the Macao Online Map base data was updated to June 2025, while the current statutory coastline is based on a May 1, 2025 field survey. Neighboring Zhuhai land is built from pinned OpenStreetMap coastline ways dated 2026-09-20 intersected with Xiangzhou administrative relation 5405551 dated 2026-09-21. The raw Zhuhai polygon overlaps the protected Macau target by ~1,988.1 SVG px²; the exact target-negative `foreign-land-only` clip therefore makes the shared land interface follow the protected official Macau outline rather than guessing or smoothing a cross-source border. It does not extend Zhuhai beyond the OSM/Xiangzhou source, and native-size review preserves the water channel along the western peninsula and the separate Taipa/Cotai/Coloane coast. DSSCU's official land-boundary description independently confirms the Guangdong–Macao interface consists of the Border Gate section and Canal dos Patos section, consistent with the rendered topology. The final context path is source/hash-guarded in `tests/test_map_context_exception_coasts.py`. This is a PASS at JOURNEY ATLAS map scale, not a legal boundary survey.

**Antigua & Barbuda / Malta:** removal of generated self-island duplicates and Malta's no-foreign-land no-op do not establish the accuracy of every residual neighboring polygon. Preserve approved country paths.

**Remaining candidates, including El Salvador:** each map needs full-size geographic review with independent coast/border provenance and an individual disposition. A fresh scan of the 94 stored candidates found 28 with at least one generated polygon edge longer than 60 SVG px entirely inside the canvas, including El Salvador. This is a **triage signal, not proof of error**: true inland boundaries and valid low-detail coasts can have long segments. El Salvador's native-size preview shows several small pale polygon slivers away from the target coastline; their ownership has not been established. Neither a four-contact-sheet scan nor SHA/PNG/browser QA can confer a PASS on the other countries. Keep all geographically unverified candidates on HOLD.

## Stage gates

**Stage ②:** For every selected map, record source name/version/license and actual geographic extent, independently check each native 1200×760 composition (mainland, islands, exclaves, adjacent land, all insets), compare source vintages and the approved national paths, verify exact path/marker/source SHA preservation, and resolve or explicitly obtain approval for any change in scope. A rejected candidate is an unresolved requested map, **not** a geographic PASS or a completed rollout. Test affected real pages across Desktop / Tablet / Mobile and source credit. The stager `scripts/stage_map_context_rollout.py` does not exclude rejected candidates and its batch artifact is only for read-only QA: every entry now explicitly records `geographicQaStatus: "HOLD"` and `promotionEligible: false`; the existing map-context preflight asserts both values. **This is not a mechanism for geographic approval or production promotion.**

**Stage ③:** Only after Stage ② source-backed disposition and approval for the entire requested roster; regenerate exact current-main roster and SHA; implement reviewed changes on the draft branch, then verify exact affected assets, real pages, markers, accessibility, licenses and unchanged publication. Do not silently omit a country.

**Stage ④:** Only after explicit user final approval: merge/deploy and verify deployed SHA, live page URLs, navigation, `atlasPublished`, robots/noindex and sitemap. Never publish an unapproved Country.

## Prepared scripts and original evidence

`scripts/add_country_map_context.py` provides the common context generator; `scripts/add_country_map_context_existing.py` and `scripts/add_country_map_context_legacy.py` adapt existing approved SVG structures; `scripts/filter_duplicate_target_context.py` suppresses certain proved duplicates **in generated context only**, not source geometry; `scripts/stage_map_context_rollout.py` stages outside the repository with a source/output SHA manifest and does not modify production. The dated pre-closure detailed report preserves Monaco diagnostic source SHAs, official Singapore/Macao data leads, Portuguese clipping geometry, GSHHG/ODbL provenance and historical CI links: [read it here](https://github.com/yagenji/Journey-Atlas/blob/54c8c7f26abbfc4f25264b65f529cef693ab9825/docs/MAP_CONTEXT_ROLLOUT.md). No generation or test greenlight replaces the Stage ② checks above.
